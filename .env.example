# JUSAY website — client environment variables
#
# Both values below are safe to expose in the browser: the anon key is a public
# key protected by Supabase Row Level Security. Never put a service-role key here.
#
# The app also ships these same values as inline fallbacks in src/lib/supabase.ts,
# so the site keeps working on Vercel even before these vars are configured.
# Set them in Vercel → Project → Settings → Environment Variables to override.

VITE_SUPABASE_URL=https://rrromegwhhkyjsfxvesu.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InJycm9tZWd3aGhreWpzZnh2ZXN1Iiwicm9sZSI6ImFub24iLCJpYXQiOjE3NzEyMjM1NDIsImV4cCI6MjA4Njc5OTU0Mn0.m0bJCOLoBFCMnFFhb2SaKoYandShMLxJ90etIDewErE
